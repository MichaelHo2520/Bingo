# === Bingo 公開 repo：只放程式(index.html / styles.css / js/ / mp3/*.mp3)===
# 其餘一律不上傳；文檔改用 notes/ 底下的獨立 git 做本機版本控管。

# 本機文檔與計畫(notes/ 內另有獨立 git,永不 push 到此公開 repo)
notes/

# 所有 Markdown 文檔都不上傳(要公開某份時用 git add -f 強制加入)
*.md
# 例外:README.md 要公開在 GitHub 首頁(給人搜尋/瀏覽),放行上傳
!README.md

# 開發用工具腳本(連線雙人測試),不進公開 repo
tools/

# 設計參考圖與舊資產(第三方素材,只用來對照配色與排法,絕不上傳也不打包進 App)
# 一律收在 design-refs/,根目錄不再散放圖片;裡面目前有:
#   麻將花色對照表-雀神.webp   v1.55.0 的麻將牌面照這張「認識麻將花色」表重畫
#   數獨icon.png              v1.101.2 首頁數獨木框盤面 + 鉛筆的參考圖
#   暗棋icon-帥與蓋牌-插畫.png  v1.145.0 暗棋首頁 icon 的原圖(兩顆棋子從它切出來)
#   台灣麻將icon-台灣牌-插畫.png v1.148.0 台灣麻將首頁 icon 的原圖
#   十一遊戲icon總圖-插畫.png   ★ v1.149.0 十一個遊戲的圖示全部從這一張切出來
#                             (tools/cut-icons.py 演算法裁切,可重跑)
#   back.jpg                  早期實驗殘留,沒有任何頁面引用
# ⚠ v1.149.0 把這裡的檔名全部改成看得懂的中文(原本是 file_00000000xxxx.png 那種亂碼)。
# ⚠ 真正在用的圖不在這裡,收在 **img/**(v1.176.0 從專案根搬過去;黑名單制 → 自動上傳):
#   img/icon.svg      PWA 圖示(十三頁 + manifest.json + sw.js 都引用它)
#   img/<前綴>-icon.png  ★ 十二個遊戲的圖示(v1.149.0;bc gk sk mk m16 sv b2 bj un dc cy dw)——
#                 每一張都是「index.html 首頁卡 + 該遊戲進場插圖」同一個檔案,而且列進 sw.js 的 CORE
# ⚠⚠ 頁面**絕對不可以**直接指向 design-refs/ —— 那是「本機好好的、線上永遠破圖」。
design-refs/

# 本機專用倉庫的 git 目錄(v1.159.0)。管的是「不上 GitHub 但值得版控」的東西:
#   CLAUDE.md + tools/ 的原始碼(在此之前那兩者完全沒有版控 —— 上面 *.md 與 tools/ 把它們濾掉了)
# 它的 work-tree 也是專案根,與這個公開 repo 重疊,靠各自的 index 分開;**永遠沒有 remote**。
# 用法見 CLAUDE.md 檔尾與 tools/lg.ps1。
.git-local/

# 系統/編輯器雜項
.DS_Store
Thumbs.db
