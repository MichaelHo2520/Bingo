# === Bingo 公開 repo：只放程式(index.html / styles.css / js/ / mp3/*.mp3)===
# 其餘一律不上傳；文檔改用 notes/ 底下的獨立 git 做本機版本控管。

# 本機文檔與計畫(notes/ 內另有獨立 git,永不 push 到此公開 repo)
notes/

# 所有 Markdown 文檔都不上傳(要公開某份時用 git add -f 強制加入)
*.md
# 例外:README.md 要公開在 GitHub 首頁(給人搜尋/瀏覽),放行上傳
!README.md

# 開發用工具腳本(連線雙人測試),不進公開 repo
tools/

# 設計參考圖與舊資產(第三方素材,只用來對照配色與排法,絕不上傳也不打包進 App)
# 一律收在 design-refs/,根目錄不再散放圖片;裡面目前有:
#   970_*.png.webp  v1.55.0 的麻將牌面照這張「認識麻將花色」表重畫
#   數獨icon.png    v1.101.2 首頁數獨木框盤面 + 鉛筆的參考圖
#   back.jpg        早期實驗殘留,沒有任何頁面引用
# ⚠ 真正在用的 icon.svg 不在這裡(九頁 + manifest.json + sw.js 都引用它),留在根目錄。
design-refs/

# 系統/編輯器雜項
.DS_Store
Thumbs.db
